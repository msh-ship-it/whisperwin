# WhisperWin

Локальный voice-to-text для windows на `mlx-whisper`.

## Что это

`WhisperWin` записывает голос с микрофона, транскрибирует локально и вставляет текст в активное приложение.

Просто скачай и запусти exe
